-r requirements.txt

# Deployment helpers and mocked AWS clients used by the test suite.
boto3
botocore

# Imported by web.cache_warmer during cache behavior tests. The full web
# dependency set is intentionally not installed in the lightweight CI job.
sqlalchemy